//详细显示日志：
git log --oneline --all --graph

//查看标签
git tag

//创建标签
git tag v0.1

//将本地的标签全部推送到远程标签
git push origin --tags

//查看是否被修改（修改过的那个序列码是会改变的）
sha256sum 文件名.后缀名

//将某次提交的序列号和标签挂上钩（只需复制序列号前几位不同的就行一般是前七位）
git tag v3 83dd344		//v3是标签名 83dd344是某次提交的序列号
